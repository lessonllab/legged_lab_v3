# G1 Instinct 视觉训练检查：2026-09-10 下午

检查运行：`2026-09-10_10-58-20_instinct_vision_v1`，任务
`LeggedLab-Isaac-AMP-Depth-Instinct-G1-v0`，512 环境、80000 轮预算。
检查时运行约 5 小时，日志约 10300 轮。训练保持运行，本次未调整配置。

## 判断

已经学到持续前进，地形课程也在升级；没有复现旧模型长期停在等级 0、不前进的问题。
但尚未达到稳定速度/转向跟踪，AMP 风格匹配仍然偏弱，不能宣称完成崎岖路面行走验收。

## 日志证据

以下为首次读取时最近 500 轮的均值，避免依赖单个波动点：

- 平均地形等级 5.59（共 10 行，编号 0–9）。临近第 3000/5000/7000 轮的窗口均值分别约
  0.23/1.25/4.08，表明难度确实提高。升级判据是离起点距离超过 4 米，不能证明方向跟踪准确。
- 平均回合长度 729 步，控制周期 0.02 秒，即约 14.6 秒；回合上限 20 秒。
- time_out 日志约 59%；base_height 约 23%，base_contact 约 21%。
  这是日志窗口均值，终止条件可能重叠，不能相加当作互斥的精确失败率。
- 平移速度误差 0.539 m/s，转向误差 1.683 rad/s；最近严格命令跟踪成功率为 0。
  成功定义为整回合平均平移误差 <0.5 m/s 且转向误差 <0.4 rad/s，不是障碍通过率。
- 动作标准差保持 0.5，没有继续膨胀；总奖励从初始负值提高至最近窗口约 6.34。
- AMP 在线判别器输出约 -0.896，参考动作输出约 +0.897；风格奖励约 0.00646/步，
  约 35% 采样步为零。真实与生成动作仍容易区分，风格模仿不足，不能将判别器损失低解读为动作质量高。

窗口趋势仍有改善：最近 500 轮相比前 500 轮，平均回合长度约 717→729 步，
平移误差 0.558→0.539 m/s；转向误差约 1.679→1.683，几乎没有改善。

## 第 10000 轮独立检查

使用 `model_10000.pt`，seed=42，32 个首回合，等级固定 0，速度指令 0.5 m/s、转向 0。
使用确定性动作，关闭观测噪声、质量/摩擦随机化；保留新版深度处理和参考状态重置。
终点在自动重置前记录。与有探索噪声、混合命令和更高等级地形的训练日志不是同一测试条件。

- 30/32 回合走满 20 秒，30/32 超过 4 米净位移，均为 93.75%。
- 全部回合平均净位移 12.27 米、平均路径长度 13.58 米；2 个提前失败。
- 全部回合平均前向速度 0.634 m/s；30 个存活回合均值 0.714 m/s，高于 0.5 m/s 指令。
- 平均 XY 速度误差 0.305 m/s，平均转向误差 0.578 rad/s，说明仍有超速、横向或转向偏差。

这是单种子、小样本、初始等级 0 的检查。长距离行走可离开初始 8×8 米地形块，
因此它衡量从该等级起步的实际前进表现，不等于严格限定地形块内的障碍通过率。
尚未独立检查等级 5–9，也没有进行深度遮蔽/打乱消融，不能据此证明视觉的因果贡献。

## 下一步

保留当前训练和 checkpoint，不需要因之前等级 0 的问题重启。
下一轮应优先检查高等级地形、不同转向指令，以及正常/遮蔽深度的表现差异；
若转向误差和 AMP 风格指标继续不改善，再针对这些短板做对照实验，而不是只延长训练到 80000 轮。

原始指标：[training_snapshot.json](training_snapshot.json)。
独立测试记录及 checkpoint 哈希：[eval_seed42.json](eval_seed42.json)。
